<div class="container d-flex justify-content-center align-items-center flex-column full-height">
  <h1 class="mb-5">To-Dos with Hotwire!</h1>

  <%= turbo_stream_from "to_dos" %>

  <div class="w-50">
    <%= render 'form', to_do: @to_do %>
  </div>
  <div class="card align-self-stretch min-vw-50 mx-5">
    <div class="card-body">
      <div class="d-flex align-items-center justify-content-center py-5" data-controller="to-dos--zero-pending-banner">
        <h4 class="text-secondary">You have <strong class="text-primary">zero</strong> To-Dos left! 🎉🎉🎉</h4>
      </div>
      <ul class="list-group" id="to_do_list">
        <%= turbo_frame_tag "to_dos" do %>
          <%= render @to_dos %>
        <% end %>
      </ul>
    </div>
  </div>
</div